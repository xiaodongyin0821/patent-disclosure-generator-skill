# 参赛说明：专利交底技能包

## 这到底是什么

这是一个 **Skill 技能赛道作品**，名称：

**Patent Disclosure Generator：中文发明专利技术交底书生成器**

它不是一个独立 SaaS，也不是要替代专利代理人的“AI 代理人”。它的定位更窄、更实用：

> 给智能助手增加一套稳定的专利交底工作流，让其在处理发明想法、算法代码、Agent 项目实现、工程说明、附图和旧版交底书时，能够按“材料判断 -> 主线提炼 -> 追问补全 -> 正文生成 -> 图文公式审校 -> 迭代优化”的流程工作。

## 为什么以 Skill 形式参赛

因为这个场景的真实价值不在“做一个聊天壳”，而在把专业流程固化为可复用能力。

直接和通用模型或普通 Agent 对话当然也能写交底书，但效果依赖用户会不会提问；用户一旦没说明“不要编造实验数据”“先区分核心发明和工程补丁”“检查图号术语”，模型就可能直接写出一篇看似完整但事实缺口被掩盖的文档。

这个技能包的价值是把专业要求前置到工作流中，让智能助手默认执行这些动作。

它尤其适合一种很常见但容易被忽视的情况：项目已经借助 Agent 或复杂算法开发完成，效果很好，也明显有专利价值，但因为原理链路长、模块耦合强、兼容场景复杂，用户自己很难把真正值得保护的发明点讲清楚。这个 skill 就是为这种“先做成，再抽主线”的路径准备的。

## 它解决的具体问题

### 1. 用户不会问

研发人员通常会说：“帮我把这个算法写成专利交底书。”

但他没有说清楚：

- 现有技术缺陷是什么
- 核心区别特征是什么
- 哪些步骤是必要特征
- 哪些只是工程补丁
- 技术效果有没有数据支撑
- 附图和正文是否一致

skill 会主动按这些维度梳理。

### 2. 复杂算法很难反推专利主线

在大模型和 Agent 时代，很多技术创新不是先写专利、再做实现，而是先把项目做成，之后才发现其中某些机制很有专利价值。

难点在于：

- 真正有价值的常常不是表层功能，而是模块之间的依赖关系
- 复杂场景兼容、鲁棒恢复、反馈闭环往往藏在长实现链路里
- 用户自己也不容易区分“可保护原理”和“普通工程实现”

skill 会帮助用户从既有算法、Agent 任务链或系统流程中，反向提炼真正值得写进专利交底书的技术主线。

### 3. 模型容易直接成文

普通对话中，模型为了满足“写一份交底书”的请求，可能直接补全很多用户没给的内容。

skill 会先判断材料成熟度：

- A 类：可直接起草
- B 类：需补充后起草
- C 类：仅能先梳理问题

材料不足时先追问，不硬写最终稿。

### 4. 交底书容易写成代码说明

很多技术材料来自代码或工程实现，直接写会变成“步骤堆砌”。

skill 会把内容分三层：

- 核心发明层
- 优选实施层
- 工程实现层

主线优先写核心发明层，工程补丁不喧宾夺主。

### 5. 公式、参数、附图容易不一致

skill 内置了公式审校、图文一致、最终审校等参考规则，可以在需要时检查：

- 公式变量是否定义完整
- 每个阶段输入输出是否清楚
- 图号是否按顺序出现
- 附图说明是否和正文一致
- 术语是否前后一致

## 和直接使用通用 Agent 的区别

| 维度 | 直接使用通用 Agent | 使用本技能包 |
|---|---|---|
| 用户门槛 | 需要用户自己会提示 | 用户只需给技术材料 |
| 工作流 | 容易一次性成文 | 默认先判断材料成熟度 |
| 缺失信息 | 可能被模型润色掩盖 | 显式列出待补材料和追问 |
| 发明主线 | 依赖用户提示 | 默认提炼问题-方案-效果闭环 |
| 工程补丁 | 容易写进主线 | 默认分层处理 |
| 公式/附图 | 需要额外要求检查 | 有专门审校规则 |
| 边界 | 容易被误用成法律判断 | 明确只做技术交底辅助 |

## 典型使用流程

### 第一步：触发 skill

用户说：

```text
使用 patent-disclosure-generator，帮我把这段算法说明整理成中文发明专利技术交底书。
```

或：

```text
请基于这份旧版交底书继续优化，重点补全公式、参数说明和图文一致性。
```

或：

```text
这个 Agent/算法项目已经做完了，请从现有实现里帮我抽取有专利价值的技术主线，再整理成交底材料。
```

### 第二步：输入材料

可以输入：

- 算法代码
- Agent 工作流、模块说明、工具链描述
- 工程流程说明
- 旧版交底书
- 实验结果
- 附图说明
- 零散发明想法

### 第三步：skill 先判断材料状态

输出类似：

```text
材料成熟度：B 类，需补充后起草。
已知主线：...
关键缺口：...
建议先确认 4 个问题：...
```

### 第四步：用户补充

用户回答关键问题。

### 第五步：生成交底材料

skill 输出：

- 技术交底书草稿
- 技术问题-技术方案-技术效果表
- 核心发明点/优选实施/工程补丁分层表
- 公式与参数说明
- 附图说明
- 待代理人确认的问题

### 第六步：迭代审校

用户可以继续要求：

```text
检查图号、公式变量和术语是否一致。
```

或：

```text
哪些内容像工程补丁，不适合作为主发明点？
```

## 当前目录怎么用

真正的技能目录在：

`D:\models\TX-AI Agent\patent-disclosure-generator`

核心文件：

- `patent-disclosure-generator\SKILL.md`：skill 主说明
- `patent-disclosure-generator\references\输出模板.md`：交底书输出骨架
- `patent-disclosure-generator\references\公式审校.md`：公式与参数审校
- `patent-disclosure-generator\references\附图规则.md`：图文一致规则
- `patent-disclosure-generator\references\专利性主线提炼.md`：专利性主线提炼
- `patent-disclosure-generator\references\最终审校.md`：最终审校

比赛提交材料：

- `比赛提交文案.md`：提交页复制版
- `演示流程.md`：演示流程与样例

## 如何证明它有价值

演示时不要只展示“生成一篇交底书”。要展示三点：

1. 同样的材料，如果信息不足，skill 不会硬写最终稿，而是先指出缺口。
2. 对已完成的复杂算法或 Agent 项目，skill 能从实现链路里反向抽取真正值得保护的技术主线。
3. skill 能把算法/代码材料分成核心发明、优选实施、工程补丁，并继续审校公式、图号、术语和技术问题闭环。

这样才能体现它不是普通文案生成，而是一个专利交底专业工作流。
