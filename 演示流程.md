# 演示流程：专利交底技能包

## 演示目标

证明它不是“一个交底书模板”，而是一个会判断材料成熟度、会追问缺口、会提炼主线、会审校图文公式的专利交底技能包。

## 演示输入

把下面内容作为用户输入给智能助手，并要求使用 `patent-disclosure-generator`：

```text
使用 patent-disclosure-generator，帮我把下面这个算法想法整理成中文发明专利技术交底材料。先不要急着写最终版，如果材料不够，请先指出缺口并追问。

我们做了一个工业显微图像里的缺陷检测算法。传统模板匹配在亮度变化和局部旋转时容易误检。我们的办法是先做图像归一化，然后找候选区域，再做多尺度多角度模板匹配。之后用几何一致性投票，把孤立匹配点去掉。最后根据高置信度结果更新模板参数。

模块包括：预处理、候选区域生成、模板匹配、投票筛选、模板更新、结果输出。

目前实验上看误检少了，但还没有整理完整数据。准备画三张图：系统结构图、流程图、投票示意图。
```

## 期望第一轮输出

技能包不应该直接长篇写完整交底书，而应先输出类似内容：

```text
材料成熟度：B 类，需补充后起草。

已知技术事实：
1. 应用场景是工业显微图像缺陷检测。
2. 现有模板匹配在亮度变化和局部旋转时容易误检。
3. 方案包括归一化、候选区域生成、多尺度多角度模板匹配、几何一致性投票和模板更新。

可能发明主线：
通过多尺度多角度匹配与几何一致性投票结合，并基于高置信度结果更新模板参数，从而提升模板匹配在亮度变化和局部旋转场景下的稳定性。

关键缺口：
1. 几何一致性投票的规则不清楚。
2. 模板参数具体包括哪些内容不清楚。
3. 模板更新触发条件不清楚。
4. “误检少了”缺少实验数据和对比基线。
5. 候选区域生成依据的特征不清楚。

建议先确认：
1. 投票规则是否基于距离、角度、尺度或拓扑关系？
2. 模板更新参数包括尺度、角度、亮度还是特征权重？
3. 哪些高置信度结果可以用于模板更新？
4. 是否有误检率、漏检率或准确率对比数据？
```

## 演示补充输入

继续输入：

```text
补充：几何一致性投票主要基于匹配点之间的相对距离和角度关系，偏离邻域平均关系太大的点会被剔除。模板参数包括尺度、旋转角度、亮度归一化系数和边缘特征权重。只有连续 3 张图像中都稳定出现的高置信度匹配结果才参与模板更新。实验数据暂时只能写定性效果，不能写具体百分比。

请继续生成交底材料，但不要编造实验数据。
```

## 期望第二轮输出结构

```text
一、技术问题-技术方案-技术效果表
二、核心发明点/优选实施/工程补丁分层表
三、中文发明专利技术交底书草稿
四、附图说明
五、仍需代理人或研发确认的问题
```

其中应体现：

- 不写具体误检率百分比。
- 把“连续 3 张图像稳定出现”写成优选实施条件，而不是绝对限制。
- 把几何一致性投票和模板更新作为主线。
- 把图 1、图 2、图 3 与正文阶段对应起来。

## 可展示的关键片段

### 1. 技术问题-方案-效果表

| 项目 | 内容 |
|---|---|
| 技术问题 | 固定模板匹配在亮度变化、局部旋转和微小尺度变化下易产生误检 |
| 技术方案 | 对候选区域进行多尺度多角度匹配，并通过几何一致性投票筛除孤立匹配点，再基于高置信度结果更新模板参数 |
| 技术效果 | 提升模板匹配在复杂成像条件下的稳定性，降低误检风险；具体效果需以后续实验数据支撑 |

### 2. 分层表

| 层级 | 内容 | 写法建议 |
|---|---|---|
| 核心发明层 | 多尺度多角度匹配、几何一致性投票、基于高置信度结果的模板更新 | 写入发明主线 |
| 优选实施层 | 连续 3 张图像稳定出现才更新模板 | 写成优选条件，不写死为唯一条件 |
| 工程实现层 | 结果可视化、批处理、路径保存 | 可不进入主线 |

### 3. 图文一致检查

| 图号 | 图题 | 对应正文 |
|---|---|---|
| 图1 | 系统结构图 | 预处理、候选区域、模板匹配、投票筛选、模板更新、结果输出模块 |
| 图2 | 方法流程图 | S1-S6 方法步骤 |
| 图3 | 几何一致性投票示意图 | 匹配点距离、角度一致性判断 |

## 和直接通用 Agent 对话的对比演示

可以口播：

“如果直接让通用模型写，它可能会马上生成一篇完整交底书，甚至把未给出的实验效果写得很确定。本技能包的区别是，它先判断材料是否足够，把缺口显式列出来；补充后再起草，并且把核心发明、优选实施和工程补丁分层处理。”

## 3 分钟录屏脚本

### 0:00-0:25 说明作品

“这是 Patent Disclosure Generator，一个中文发明专利技术交底书生成技能包。它不是替代专利代理人，而是让智能助手按专利交底工作流处理算法、代码、附图和旧版草稿。”

### 0:25-0:55 展示输入

粘贴第一段不完整算法材料。

### 0:55-1:35 展示第一轮输出

重点展示：材料成熟度、已知事实、关键缺口、追问问题。

### 1:35-2:05 补充信息

粘贴第二段补充材料。

### 2:05-2:45 展示第二轮输出

重点展示：技术问题-方案-效果表、分层表、交底书草稿、附图说明。

### 2:45-3:00 总结价值

“它的价值不是多写一篇文档，而是把专利交底经验变成可复用技能包，让智能助手默认会追问、会分层、会审校，减少技术材料整理过程中的遗漏和返工。”
